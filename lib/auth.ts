/**
 * Demo authentication constants. For now the app ships with a single
 * hardcoded account — replace this with a real auth provider later.
 */
export const AUTH_COOKIE = "sl_auth";

export const DEMO_USERNAME = "admin";
export const DEMO_PASSWORD = "admin";
